- stringify: split slices and sorted maps across lines, see `p.printValue(reflect.ValueOf(f)` and `go doc -src -u fmt printvalue`
- flag: add -template=key flag for choosing a "template" to use on lhs side for new rhs keys instead of the empty value
